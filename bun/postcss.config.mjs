const config = {
  plugins: ["@tailwindcss/postcss"],
  // plugins: {
  //   tailwindcss: { config: "./tailwind.config.mjs" }, // 显式指定配置文件路径
  //   autoprefixer: {},
  // },
};

export default config;
